paths:
  data_root: null
  split_root: null
protocol:
  seeds: [42, 2026, 3407]
  folds: [0, 1, 2, 3, 4]
  epochs: 100
  encoder_learning_rate: 0.000003
  classifier_learning_rate: 0.0001
  dropout: 0.5
  label_smoothing: 0.1
  batch_size: 8
  optimizer: AdamW
  weight_decay: 0.05
  gradient_clip: 1.0
  scheduler: cosine
  eta_min: 0.000001
  checkpoint_selection: validation_balanced_accuracy
  final_test_policy: once_after_selected_checkpoint_reload
